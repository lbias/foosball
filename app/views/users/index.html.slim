.row
  = link_to 'New user', new_user_path, class: 'btn right'
  h4 Users list
table.responsive-table.bordered
  thead
    tr
      th Name
      th Some Statistics
      th
  tbody
    - @users.each do |user|
      tr
        td
          = link_to user.name, user
        td
        td.right-align
          => link_to content_tag(:i, 'edit', class: 'material-icons'), [:edit, user], class: 'btn-floating', title: 'Edit'
          = link_to content_tag(:i, 'delete', class: 'material-icons'), user, method: :delete, class: 'btn-floating red', title: 'Delete'
